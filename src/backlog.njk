<div class="govuk-o-width-container app-c-site-width-container">
    <div class="govuk-o-grid govuk-o-main-wrapper">
      <div class="govuk-o-grid__item govuk-o-grid__item--two-thirds">
          
          <h2 class="govuk-heading-xl">Design System Backlog</h2>

          <p class="govuk-body">This is the backlog of styles, components and patterns that the government design community is currently working on. Select an item to find out more on GitHub.</p>


        {% set columns = backlog.data.repository.project.columns.nodes %}
        {% set proposed = columns[1].cards.edges %}
        {% set agreed = columns[2].cards.edges %}
        {% set inProgress = columns[3].cards.edges %}


        <table class="govuk-c-table">
          <tr>
            <th class="govuk-c-table__header">Name</th>
            <th class="govuk-c-table__header">Status</th>
          </tr>

          {% for card in inProgress %}
          <tr>
          {% if card.node.content.title %}
            <td class="govuk-c-table__cell">
              <a href="{{ card.node.content.url }}">{{ card.node.content.title }}</a>
            </td>
            <td class="govuk-c-table__cell">
              In progress
            </td>
          {% endif %}
          </tr>
          {% endfor %}

          {% for card in agreed %}
          <tr>
          {% if card.node.content.title %}
            <td class="govuk-c-table__cell">
              <a href="{{ card.node.content.url }}">{{ card.node.content.title }}</a>
            </td>
            <td class="govuk-c-table__cell">
            </td>
          {% endif %}
          </tr>
          {% endfor %}
          
        </table>


        <h3 class="govuk-heading-l">Proposals</h3>

        <p class="govuk-body">The following styles, components and patterns have been proposed by the community but have not yet been added to the backlog.</p>

        <p class="govuk-body"><a href="#">Propose a new style, component or pattern</a></p>

        <table class="govuk-c-table">
          <tr>
            <th class="govuk-c-table__header">Name</th>
          </tr>

          {% for card in proposed %}
          <tr>
          {% if card.node.content.title %}
            <td class="govuk-c-table__cell">
              <a href="{{ card.node.content.url }}">{{ card.node.content.title }}</a>
            </td>
          {% endif %}
          </tr>
          {% endfor %}
          
        </table>

      </div>
  </div>
</div>
